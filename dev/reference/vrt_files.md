# List the files that a vrt is built on

List the files that a vrt is built on

## Usage

``` r
vrt_files(vrt, omit_vrt = FALSE)
```

## Arguments

- vrt:

  path to a .vrt file

- omit_vrt:

  omit the listing of the original vrt. Default `FALSE`

## Value

character vector of tiles
